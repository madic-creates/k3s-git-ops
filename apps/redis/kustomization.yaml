---
commonAnnotations:
  argocd.argoproj.io/sync-wave: "09"

helmCharts:
  - name: redis
    repo: oci://registry-1.docker.io/bitnamicharts
    version: 24.0.4
    releaseName: redis
    namespace: databases
    valuesFile: values.yaml

resources:
  - k8s.np.redis.yaml
