apiVersion: v1
kind: Service
metadata:
  name: tftpd-hpa
  namespace: media
  annotations:
    argocd.argoproj.io/sync-wave: "30"
  labels:
    app.kubernetes.io/name: tftpd-hpa
spec:
  type: NodePort
  ports:
    - port: 69
      targetPort: 69
      protocol: UDP
      name: tftp
      nodePort: 30069
  selector:
    app.kubernetes.io/name: tftpd-hpa
