apiVersion: v1
kind: Service
metadata:
  name: ssl-exporter
  namespace: monitoring
  annotations:
    argocd.argoproj.io/sync-wave: "20"
  labels:
    app.kubernetes.io/name: ssl-exporter
spec:
  type: ClusterIP
  ports:
    - port: 9219
      targetPort: 9219
      protocol: TCP
      name: metrics
  selector:
    app.kubernetes.io/name: ssl-exporter
