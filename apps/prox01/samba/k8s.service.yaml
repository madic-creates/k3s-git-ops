apiVersion: v1
kind: Service
metadata:
  name: samba
  namespace: media
  annotations:
    argocd.argoproj.io/sync-wave: "30"
  labels:
    app.kubernetes.io/name: samba
spec:
  type: NodePort
  ports:
    - port: 445
      targetPort: 445
      protocol: TCP
      name: smb
      nodePort: 30445
  selector:
    app.kubernetes.io/name: samba
