apiVersion: v1
kind: Service
metadata:
  name: snmp-exporter
  namespace: monitoring
  annotations:
    argocd.argoproj.io/sync-wave: "20"
  labels:
    app.kubernetes.io/name: snmp-exporter
spec:
  type: ClusterIP
  ports:
    - port: 9116
      targetPort: 9116
      protocol: TCP
      name: metrics
  selector:
    app.kubernetes.io/name: snmp-exporter
