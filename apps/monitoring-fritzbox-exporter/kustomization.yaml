apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

commonAnnotations:
  argocd.argoproj.io/sync-wave: "17"

generators:
  - kustomize-secret-generator.yaml

resources:
  - k8s.deployment.yaml
  - k8s.service.yaml
  - k8s.servicemonitor.yaml
  - k8s.np.fritzbox-exporter.yaml
