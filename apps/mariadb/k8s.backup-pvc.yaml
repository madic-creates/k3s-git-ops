# Persistend Volume
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: longhorn-pvc-mariadb-backup
  namespace: databases
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 4Gi
