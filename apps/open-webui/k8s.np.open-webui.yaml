---
# Open WebUI application-specific network policy.
# Selecting the pod via endpointSelector puts it under Cilium default-deny;
# only explicitly allowed traffic passes.
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: open-webui
  namespace: open-webui
spec:
  endpointSelector:
    matchLabels:
      app.kubernetes.io/name: open-webui
  ingress:
    # Traefik forwards browser + IDE-API traffic.
    - fromEndpoints:
        - matchLabels:
            k8s:app.kubernetes.io/instance: traefik-kube-system
            k8s:io.kubernetes.pod.namespace: kube-system
      toPorts:
        - ports:
            - port: "8080"
              protocol: TCP
    # Same-namespace traffic (probes, helm post-install hooks).
    - fromEndpoints:
        - matchLabels:
            k8s:io.kubernetes.pod.namespace: open-webui
  egress:
    # DNS via CoreDNS.
    - toEndpoints:
        - matchLabels:
            k8s:io.cilium.k8s.namespace.labels.kubernetes.io/metadata.name: kube-system
            k8s:io.kubernetes.pod.namespace: kube-system
            k8s:k8s-app: kube-dns
      toPorts:
        - ports:
            - port: "53"
              protocol: UDP
            - port: "53"
              protocol: TCP
    # llama-cpp backend (in-cluster).
    - toEndpoints:
        - matchLabels:
            k8s:app: llama-cpp
            k8s:io.kubernetes.pod.namespace: llama-cpp
      toPorts:
        - ports:
            - port: "8080"
              protocol: TCP
    # External HTTPS: Tavily API.
    - toEntities:
        - world
      toPorts:
        - ports:
            - port: "443"
              protocol: TCP
    # OIDC discovery loopback: auth.k8s.neese-web.de DNS-resolves to the
    # Traefik LB IP; Cilium kube-proxy-replacement DNATs that to the Traefik
    # pod directly on its websecure container port 8443 before policy is
    # evaluated. So we must allow egress to the Traefik pod identity, not
    # world/host.
    - toEndpoints:
        - matchLabels:
            k8s:app.kubernetes.io/instance: traefik-kube-system
            k8s:io.kubernetes.pod.namespace: kube-system
      toPorts:
        - ports:
            - port: "8443"
              protocol: TCP
    # Cluster Redis for Open WebUI websocket pub/sub
    - toEndpoints:
        - matchLabels:
            k8s:app.kubernetes.io/name: redis
            k8s:io.kubernetes.pod.namespace: databases
      toPorts:
        - ports:
            - port: "6379"
              protocol: TCP
