apiVersion: v1
kind: Service
metadata:
  name: postfix
  namespace: media
  annotations:
    argocd.argoproj.io/sync-wave: "30"
  labels:
    app.kubernetes.io/name: postfix
spec:
  type: ClusterIP
  externalIPs:
    - 192.168.1.45
  ports:
    - port: 25
      targetPort: 25
      protocol: TCP
      name: smtp
  selector:
    app.kubernetes.io/name: postfix
