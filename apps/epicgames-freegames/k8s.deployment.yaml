apiVersion: apps/v1
kind: Deployment
metadata:
  name: epicgames-freegames
  namespace: media
  annotations:
    argocd.argoproj.io/sync-wave: "30"
  labels:
    app.kubernetes.io/name: epicgames-freegames
spec:
  replicas: 1
  revisionHistoryLimit: 3
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: epicgames-freegames
  template:
    metadata:
      labels:
        app.kubernetes.io/name: epicgames-freegames
    spec:
      nodeSelector:
        node-role/workload: zfs
      containers:
      - name: epicgames-freegames
        image: ghcr.io/claabs/epicgames-freegames-node@sha256:9466b4cd9f6c4a740ea7a7db47c71e258c5d00af1bb7c86135ef630fe2ebbd02
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
        ports:
          - containerPort: 3000
            name: http
            protocol: TCP
        volumeMounts:
          - name: config
            mountPath: /usr/app/config
      volumes:
        - name: config
          hostPath:
            path: /vol_raidz1/docker/configs/epic/config
            type: Directory
