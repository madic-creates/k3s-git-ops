apiVersion: apps/v1
kind: Deployment
metadata:
  name: epicgames-freegames
  namespace: media
  annotations:
    argocd.argoproj.io/sync-wave: "30"
  labels:
    app.kubernetes.io/name: epicgames-freegames
spec:
  replicas: 1
  revisionHistoryLimit: 3
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: epicgames-freegames
  template:
    metadata:
      labels:
        app.kubernetes.io/name: epicgames-freegames
    spec:
      nodeSelector:
        node-role/workload: zfs
      containers:
      - name: epicgames-freegames
        image: ghcr.io/claabs/epicgames-freegames-node@sha256:5d6e61feafc5fed9c39af888f38396a52129fa1a0b7b15e3a085ecffc6a04908
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
        ports:
          - containerPort: 3000
            name: http
            protocol: TCP
        volumeMounts:
          - name: config
            mountPath: /usr/app/config
      volumes:
        - name: config
          hostPath:
            path: /vol_raidz1/docker/configs/epic/config
            type: Directory
