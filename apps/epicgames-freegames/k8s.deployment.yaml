apiVersion: apps/v1
kind: Deployment
metadata:
  name: epicgames-freegames
  namespace: media
  annotations:
    argocd.argoproj.io/sync-wave: "30"
  labels:
    app.kubernetes.io/name: epicgames-freegames
spec:
  replicas: 1
  revisionHistoryLimit: 3
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: epicgames-freegames
  template:
    metadata:
      labels:
        app.kubernetes.io/name: epicgames-freegames
    spec:
      nodeSelector:
        node-role/workload: zfs
      containers:
      - name: epicgames-freegames
        image: ghcr.io/claabs/epicgames-freegames-node@sha256:f0cd728292f57934bba251ed2ecf2fd914c8584a7a87bd926bc9f66f2da9625c
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
        ports:
          - containerPort: 3000
            name: http
            protocol: TCP
        volumeMounts:
          - name: config
            mountPath: /usr/app/config
      volumes:
        - name: config
          hostPath:
            path: /vol_raidz1/docker/configs/epic/config
            type: Directory
