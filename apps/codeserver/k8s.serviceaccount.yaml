---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: codeserver
  namespace: codeserver
# code-server has no business talking to the k8s API — skip the default token
# mount. The chart references this SA by name (serviceAccount.create=false in
# helm-values.yaml), so this setting propagates to the pod.
automountServiceAccountToken: false
