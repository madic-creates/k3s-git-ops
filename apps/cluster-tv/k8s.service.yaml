apiVersion: v1
kind: Service
metadata:
  name: cluster-tv
  namespace: monitoring
  labels:
    app.kubernetes.io/name: cluster-tv
  annotations:
    argocd.argoproj.io/sync-wave: "10"
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: cluster-tv
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
