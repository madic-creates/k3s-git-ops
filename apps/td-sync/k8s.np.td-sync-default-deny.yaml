---
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: td-sync-default-deny
  namespace: td-sync
spec:
  endpointSelector: {}  # Empty selector = matches ALL pods in this namespace
  # Default deny
  ingress:
    - {}
  egress:
    - {}
