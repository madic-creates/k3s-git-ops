---
# Hubble Relay Network Policy
# Controls network access for Cilium Hubble Relay (flow aggregation service)
# Hubble Relay aggregates network flow data from all Cilium agents in the cluster
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: hubble-relay
  namespace: kube-system
  annotations:
    argocd.argoproj.io/sync-wave: "05"
spec:
  endpointSelector:
    matchLabels:
      k8s-app: hubble-relay
  ingress:
  # Allow Hubble UI to connect on gRPC port 4245
  # This is the primary interface for the Hubble UI to retrieve network flow data
  - fromEndpoints:
    - matchLabels:
        k8s:k8s-app: hubble-ui
        k8s:io.kubernetes.pod.namespace: kube-system
    toPorts:
    - ports:
      - port: "4245"
        protocol: TCP
  # Allow health checks and metrics from within kube-system namespace
  - fromEndpoints:
    - matchLabels:
        k8s:io.kubernetes.pod.namespace: kube-system
    toPorts:
    - ports:
      - port: "4222"
        protocol: TCP
  # Allow Prometheus to scrape metrics
  - fromEndpoints:
    - matchLabels:
        k8s:app.kubernetes.io/name: prometheus
        k8s:io.kubernetes.pod.namespace: monitoring
    toPorts:
    - ports:
      - port: "4222"
        protocol: TCP
  egress:
  # Allow DNS resolution via CoreDNS
  - toEndpoints:
    - matchLabels:
        k8s:io.cilium.k8s.namespace.labels.kubernetes.io/metadata.name: kube-system
        k8s:io.kubernetes.pod.namespace: kube-system
        k8s:k8s-app: kube-dns
    toPorts:
    - ports:
      - port: "53"
        protocol: UDP
      - port: "53"
        protocol: TCP
  # Allow connections to Cilium agents via hubble-peer service (port 4244)
  # Hubble Relay polls Cilium agents to aggregate network flow data
  - toEndpoints:
    - matchLabels:
        k8s:k8s-app: cilium
        k8s:io.kubernetes.pod.namespace: kube-system
    toPorts:
    - ports:
      - port: "4244"
        protocol: TCP
  # Allow communication with Kubernetes API server for service account authentication
  - toEntities:
    - kube-apiserver
    toPorts:
    - ports:
      - port: "6443"
        protocol: TCP
